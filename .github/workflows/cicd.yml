name: CICD

on: 
  workflow_dispatch:

jobs:
  code:
    runs-on: ubuntu-latest
    steps:
      - name: Clone the Code
        run: echo "Cloning the Code"

  build:
      needs: code
      runs-on: ubuntu-latest
      steps:
      - name: Build using Docker
        run: echo "Building with docker"

  test: 
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Running the test cases
        run: echo "testing the app"
    

  deploy:
    needs: [build,test]
    runs-on: ubuntu-latest
    steps:
      - name: Deploying on machine
        run: echo "Deploying the app"

  
